# Todo App Environment Configuration
# Copy this file to .env.production and fill in your actual values

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED - NO FALLBACKS)
# =============================================================================
# These values have NO fallbacks. The application will crash if not set.
DATABASE_URL=jdbc:postgresql://todo-db:5432/todo_prod
DATABASE_USERNAME=todo_prod_user
DATABASE_PASSWORD=YourStrongPassword123!

# =============================================================================
# JWT CONFIGURATION (REQUIRED - NO FALLBACK)
# =============================================================================
# This value has NO fallback. The application will crash if not set.
# Generate a strong 64-character secret: openssl rand -base64 48
JWT_SECRET=your-64-character-jwt-secret-key-here

# =============================================================================
# AWS CONFIGURATION (REQUIRED for S3 storage - NO FALLBACKS)
# =============================================================================
# These values have NO fallbacks. The application will crash if not set.
AWS_REGION=us-east-2
AWS_ACCESS_KEY=your-access-key
AWS_SECRET_KEY=your-secret-key
S3_BUCKET_NAME=your-bucket-name
S3_PREFIX=attachments

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
SERVER_PORT=8080
STORAGE_TYPE=s3
MAX_FILE_SIZE=25MB
MAX_REQUEST_SIZE=25MB

# =============================================================================
# VPS DEPLOYMENT CONFIGURATION
# =============================================================================
VPS_USER=your-vps-username
VPS_IP=your-vps-ip-address
VPS_HOST=digital-ocean
APP_DIR=/opt/todo-app

# SSH Configuration (for deployment scripts)
# Set this to the directory containing your SSH config and keys
# Examples:
# SSH_DIR=C:\Users\Christian\.ssh
# SSH_DIR=/c/Users/Christian/.ssh
# SSH_DIR=/mnt/c/Users/Christian/.ssh
SSH_DIR=

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
VITE_API_BASE_URL=http://localhost:8080/api

# =============================================================================
# SSL CONFIGURATION (Optional)
# =============================================================================
# Set to "true" to enable HTTPS with SSL certificates
SSL_ENABLED=false

# SSL Certificate Configuration (only used if SSL_ENABLED=true)
SSL_KEYSTORE=
SSL_KEYSTORE_PASSWORD=
SSL_KEYSTORE_TYPE=PKCS12
SSL_KEY_ALIAS=

# =============================================================================
# SECURITY WARNINGS
# =============================================================================
# ⚠️  CRITICAL: The following variables have NO fallback values:
# - DATABASE_URL, DATABASE_USERNAME, DATABASE_PASSWORD
# - JWT_SECRET
# - AWS_ACCESS_KEY, AWS_SECRET_KEY, S3_BUCKET_NAME
#
# If any of these are not set, the application will crash on startup.
# This is intentional for security reasons - no default/weak values allowed.
#
# Generate strong secrets:
# JWT_SECRET: openssl rand -base64 48
# Database password: Use a strong password with mixed case, numbers, and symbols
